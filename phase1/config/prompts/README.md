# Prompt Templates (Phase 4 use)

Placeholder. LLM prompt templates for relevance classification and evidence extraction will live here (added in Phase 4). Prompts are versioned alongside `extractor_version` so runs are reproducible.
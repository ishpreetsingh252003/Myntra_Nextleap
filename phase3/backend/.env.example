# Phase 3 (cleaning & relevance filtering). Rule-based mode needs no credentials.
# Optional: set GROQ_API_KEY (free, fast) OR GOOGLE_API_KEY (free Gemini) to
# enable the LLM relevance classifier as the decision-maker (keyword rules remain
# the pre-filter, EC-18). OpenAI/Anthropic are paid fallbacks.
GROQ_API_KEY=
GOOGLE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
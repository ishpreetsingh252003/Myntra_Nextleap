# Phase 3 (cleaning & relevance filtering). Rule-based mode needs no credentials.
# Optional: set ANTHROPIC_API_KEY (or OPENAI_API_KEY) to enable the LLM relevance
# classifier as the decision-maker (keyword rules remain the pre-filter, EC-18).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
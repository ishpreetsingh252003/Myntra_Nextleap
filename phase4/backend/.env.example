# Phase 4 (extraction). Rule-based mode needs no credentials.
# Optional: set ANTHROPIC_API_KEY (or OPENAI_API_KEY) to enable the LLM
# extractor as the decision-maker (rules-only fallback is fully offline).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Phase 4 (extraction). Rule-based mode needs no credentials.
# Optional: set GROQ_API_KEY (free, fast) OR GOOGLE_API_KEY (free Gemini) to
# enable the LLM extractor as the decision-maker (rules-only fallback offline).
# OpenAI/Anthropic are paid fallbacks.
GROQ_API_KEY=
GOOGLE_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
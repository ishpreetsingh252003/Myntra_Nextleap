# Phase 5 — Segmentation, Theme Clustering & Quantification

Placeholder. Build target (see `ARCHITECTURE_6_PHASE_PLAN.md` Phase 5):

- Segment classification from data (scikit-learn/hdbscan on embeddings + behaviour features).
- Theme clustering of evidence packets; cluster labels → opportunity themes.
- Quantification tables per theme/source/segment (pandas → CSV).

Exit: every segment/theme traceable to ≥ 3 evidence packets; frequency tables per source + segment.